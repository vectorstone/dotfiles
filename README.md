# dotfiles

这个仓库使用 GNU Stow 管理。每个顶层目录都对应 `$HOME` 下的一个配置包。

## Ghostty

配置文件位于 `ghostty/.config/ghostty/config`。

修改配置后，可以在 Ghostty 里重载配置：

- Linux: `Ctrl + Shift + ,`
- macOS: `Cmd + Shift + ,`

注意：并不是所有配置项都能热重载，有些只会在新开的终端里生效。

## 后续可补充

- `.zshrc`
- `.codex`
- 其他应用配置
